export default function Button({ title }) {
  return (
    <button className="bg-primary hover:bg-primary-hover transition duration-150 px-4 py-2 rounded mt-4">
      {title}
    </button>
  );
}
// class="btn btn-primary"

//
